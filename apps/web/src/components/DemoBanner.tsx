export default function DemoBanner() {
  return (
    <div className="w-full bg-yellow-400 text-black text-center py-2 font-semibold">
      DEMO MODE — All data is simulated and not saved
    </div>
  );
}
