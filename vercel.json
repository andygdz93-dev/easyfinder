{
  "buildCommand": "pnpm --filter @easyfinderai/shared build && pnpm --filter @easyfinderai/frontend build",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/"
    }
  ]
}
